<h1><%= @skill.title %></h1>

<h2>Skill Header</h2>
<p>
  <%= @skill.description %>
</p>

<h2>Activities</h2>

<%= render @skill.activeties %>
<% if can? :manage, :all %>

  <h2>Add an activity:</h2>
  <%= render :partial => 'activeties/form' %>

<% end %>

<% if can? :manage, :all %>
  <%= link_to 'Edit', edit_skill_path(@skill) %>
<% end  %>
<%= link_to 'Back', skills_path %>
