<p>
  <strong>Comment:</strong>
  <%= comment.body %>
</p>
<% if can? :manage, :all %>
  <p>
    <%= link_to 'Destroy Comment', [comment.article, comment],
                method: :delete,
                data: { confirm: 'Are you sure?' } %>
  </p>
<% end  %>